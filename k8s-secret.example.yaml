# Template — copy to k8s-secret.yaml) and replace the value.
apiVersion: v1
kind: Secret
metadata:
  name: k8s-go-secrets
  labels:
    app: k8s-go
type: Opaque
# stringData = plain text at apply time, k8s base64-encodes on store.
stringData:
  API_TOKEN: "replace-me"
