apiVersion: v1
kind: Service
metadata:
  name: go-app-service
spec:
  type: LoadBalancer     # externally accessible
  selector:
    app: go-app          # matched pod labels
  ports:
    - protocol: TCP
      port: 80           # external port
      targetPort: 8080   # pod port
